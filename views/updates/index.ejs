<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cat Updates</title>
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <h1>Cat Updates</h1>
    <% if (updates.length) { %>
        <ul>
            <% updates.forEach((update) => { %>
            <li>
                <strong><%= update.cat.name %>:</strong> <%= update.activity %>
                <% if (update.health) { %> 
                    | Health: <%= update.health %>
                <% } %>
            </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No updates available.</p>
    <% } %>
    <a href="/cats/<%= cat._id %>/updates/new">Add a new update</a>
    <%- include('../partials/footer.ejs') %>
</body>
</html>
